import * as UITypes from "./types" ; 
import * as Pages from "./Pages/definitions" ; 

export const Page = (()=>{
    return {
        new     : Pages.newPage, 
        topNav  : Pages.topNav 
    } ; 
})() ; 